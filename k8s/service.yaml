apiVersion: v1
kind: Service
metadata:
  name: fiap-mecanica
  namespace: fiap-mecanica
spec:
  type: LoadBalancer
  selector:
    app: fiap-mecanica
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
