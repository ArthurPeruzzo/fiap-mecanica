apiVersion: v1
kind: ConfigMap
metadata:
  name: fiap-mecanica-config
  namespace: fiap-mecanica
data:
  DB_URL: "jdbc:mysql://fiap-mecanica-db.crg6wdidxgew.us-east-1.rds.amazonaws.com:3306/mecanica"

  # Mapeiam para as propriedades url.aprovar.orcamento / url.recusar.orcamento
  # (src/main/resources/application.properties:16-17) via relaxed binding do
  # Spring Boot. Atualizar com o hostname real do LoadBalancer depois do
  # primeiro `kubectl apply -f k8s/` (ver `kubectl get svc -n fiap-mecanica`)
  # e reaplicar este ConfigMap.
  URL_APROVAR_ORCAMENTO: "http://a2ed2418fcd4d4b1f9a10273c194af18-386897844.us-east-1.elb.amazonaws.com/ordem-servico/orcamento/externo/aprovar/"
  URL_RECUSAR_ORCAMENTO: "http://a2ed2418fcd4d4b1f9a10273c194af18-386897844.us-east-1.elb.amazonaws.com/ordem-servico/orcamento/externo/recusar/"
